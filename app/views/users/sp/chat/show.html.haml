.chatPage
  %header.header
    = link_to "", users_sp_matching_index_path, class: "linkToMatching"
    .chatPartner
      -# .chatPartner_img= image_tag @chat_room_user.img_name.url(:thumb)
      .chatPartner_name= @chat_room_user.name
  .container
    .messagesArea.messages
      -# each文をよしなに行う。処理効率も高い
      -# = render partial: 'chat_messages/chat_message', collection: @chat_messages
      = render @chat_messages
  %form.messageInputForm
    .container
      %input.messageInputForm_input{"data-behavior" => "room_speaker", :placeholder => "メッセージを入力...", :type => "text"}
